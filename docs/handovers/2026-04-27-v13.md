# KBT Handover — v13 (mid-deploy, paused for usage)

**Date:** 2026-04-27
**Previous:** v12

## RESUME HERE

I was driving the Cloudflare Pages dashboard via Claude in Chrome MCP and got 90% through the wiring before usage ran out. The repo is fully ready. Just need ~5 minutes of dashboard clicks to finish.

### Already done

- Cloudflare GitHub App **installed on LuckDragonAsgard account** with `kbt-trivia-tools` repo selected (just saved). Already had `kbt-trial` selected too.
- Repo has everything CF Pages needs:
  - `functions/api/*.js` (8 Pages Functions: env-check, ai-text, fact-check, fal-faceswap, fal-morph, fal-inpaint, fal-rembg, generate-slides + _utils.js)
  - `wrangler.jsonc` (sets nodejs_compat, points pages_build_output_dir to `./`)
  - `_redirects` (clean URLs)
  - All static HTML/JS/assets

### Next Claude — pick up at this URL

`https://dash.cloudflare.com/a6f47c17811ee2f8b6caeb8f38768c20/pages/new/provider/github`

(Account: Luck Dragon (Main), accountId `a6f47c17811ee2f8b6caeb8f38768c20`)

### Steps remaining (~5 clicks)

1. **GitHub account dropdown** → select **LuckDragonAsgard** (NOT PaddyGallivan). On a fresh page load both should now appear; we just installed the app on LuckDragonAsgard.
2. **Search "kbt-trivia-tools"** → click that repo → **Begin setup** at bottom.
3. **Build settings page:**
   - Project name: `kbt-trivia-tools` (default is fine)
   - Production branch: `main`
   - Framework preset: **None**
   - Build command: leave empty
   - Build output directory: `/` (just slash) — the existing wrangler.jsonc handles function detection
4. **Environment variables (CRITICAL — Pages Functions need these):**
   - `FAL_KEY` — fal.ai API key (used by 6 of the 8 functions)
   - `ANTHROPIC_API_KEY` — used by fact-check.js
   - `GOOGLE_SA_JSON` — Google Service Account JSON for generate-slides
   - Set for **both Production and Preview** environments
5. **Save and Deploy** → wait ~30s for first build
6. **Test** by hitting `https://kbt-trivia-tools.pages.dev/api/env-check` — should return JSON with all keys = "YES"

### After deploy is green

7. **Custom domain** (optional): point `tools.knowbrainertrivia.com.au` (or whatever) at the pages.dev URL via Pages → Custom domains.
8. **Delete dead Vercel files from repo:**
   - `api/*.js` (8 files — superseded by functions/api/)
   - `vercel.json` (superseded by _redirects)
   - `.vercel/project.json`
   - `deploy.bat`, `deploy-now.bat`, `DEPLOY.bat`
   These can be deleted via GitHub web UI now that CF Pages is the source of truth.
9. **Update memory:** add a new memory entry pointing to the new pages.dev URL, mark Vercel as fully removed.

## Where the Pages Functions code lives

All in `functions/api/` — see `functions/README.md` for routing table. Each function uses `Response.json()` and `env.X` (Workers runtime), not `res.json()` and `process.env.X` (Vercel).

## Where I was when I stopped

In the Cloudflare Pages create flow at the GitHub account dropdown — it was still showing only PaddyGallivan because the page was cached from before the GitHub App install completed. A fresh navigate to the URL above should refresh the dropdown and show both accounts.

## Key memory references

- gh-push relay: `https://gh-push.pgallivan.workers.dev/` (POST owner/repo/path/content/message — User-Agent header required to avoid CF 1010)
- Two GitHub accounts: PaddyGallivan (outlook), LuckDragonAsgard (luckdragon.io). Repo is on LuckDragonAsgard.
- Vercel project that used to host this is gone from the Vercel account already.
